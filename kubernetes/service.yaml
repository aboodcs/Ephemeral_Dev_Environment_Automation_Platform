apiVersion: v1
kind: Service
metadata:
  name: aws-dev-env-app-service
  namespace: ephemeral-dev
spec:
  type: NodePort
  selector:
    app: aws-dev-env-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080